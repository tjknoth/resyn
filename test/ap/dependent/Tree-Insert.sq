treeInsert :: x: a -> t: Tree' <\y -> x < y> <{1}> -> t: Tree' a <\y -> x < y> <{0}>
treeInsert = \x. \t.
  match t with 
    Leaf       -> Node x Leaf Leaf
    Node n l r -> if x < n then treeInsert l else treeInsert r
